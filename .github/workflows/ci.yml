name: CI

on:
  pull_request:
  push:
    branches:
      - main
      - master

jobs:
  typecheck-and-smoke:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Runtime check
        run: npm run check:runtime

      - name: Typecheck
        run: npm run typecheck

      - name: Unit tests
        run: npm run test:unit

      - name: Prepare canonical public PDF samples for semantic acceptance
        run: npm run eval:fetch-public-samples -- --sample arxiv-attention-is-all-you-need,irs-form-w4

      - name: Acceptance tests
        run: npm run test:acceptance

      - name: Import smoke tests (npm package behavior guard)
        run: npm run test:import-smoke

      - name: Integration tests
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          OPENROUTER_KEY: ${{ secrets.OPENROUTER_KEY }}
          VERCEL_AI_GATEWAY_KEY: ${{ secrets.VERCEL_AI_GATEWAY_KEY }}
        run: npm run test:integration
